/**
 * Shared geometry types for Axis Guardian
 */

/**
 * 2D position in world coordinates (meters)
 */
export interface Position2D {
  x: number
  y: number
}

/**
 * 3D position in world coordinates (meters)
 */
export interface Position3D {
  x: number
  y: number
  z: number
}
